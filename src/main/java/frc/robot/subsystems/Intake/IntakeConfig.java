package frc.robot.subsystems.Intake;

import edu.wpi.first.math.controller.PIDController;

public class IntakeConfig {
     public static final PIDController intakePID = new PIDController(1, 0, 0);

}
